/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Stores} from './stores';

describe('Stores', () => {
  it('should create an instance', () => {
    expect(new Stores()).toBeTruthy();
  });
});
